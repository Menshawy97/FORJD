# Copy to .env and set to the dev machine's LAN IP (not localhost — on a physical device,
# localhost resolves to the phone itself, not the machine running `pnpm --filter api start:dev`).
# No secrets belong in this file — see CLAUDE.md rule 5.
API_BASE_URL=http://192.168.1.100:3000
