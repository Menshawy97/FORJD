import { PlaceholderScreen } from '@/components/placeholder-screen';

/** `s_foodSearch()` -- Phase F's own "Add food" tap target, a placeholder until Phase G ("food search and detail") replaces it, mirroring how Phase I's exercise placeholders preceded Phase J/K. */
export default function FoodSearchScreen() {
  return <PlaceholderScreen name="Food search" />;
}
