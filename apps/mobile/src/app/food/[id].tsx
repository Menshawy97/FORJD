import { PlaceholderScreen } from '@/components/placeholder-screen';

/** `s_foodDetail()` -- the dashboard's item-row tap target, a placeholder until Phase G builds the real screen. */
export default function FoodDetailScreen() {
  return <PlaceholderScreen name="Food detail" />;
}
