import { PlaceholderScreen } from '@/components/placeholder-screen';

/** `s_nutritionShare()` -- the dashboard's share icon target, a placeholder until Phase I builds the real screen. */
export default function NutritionShareScreen() {
  return <PlaceholderScreen name="Share nutrition" />;
}
