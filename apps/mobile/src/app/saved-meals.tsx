import { PlaceholderScreen } from '@/components/placeholder-screen';

/** `s_savedMeals()` -- the dashboard's "star" icon and "See all" link target, a placeholder until Phase G builds the real screen. */
export default function SavedMealsScreen() {
  return <PlaceholderScreen name="Saved meals" />;
}
